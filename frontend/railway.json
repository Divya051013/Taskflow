{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "staticDir": "build",
    "startCommand": "serve -s /app/build -l tcp://0.0.0.0:${PORT:-3000}"
  }
}
